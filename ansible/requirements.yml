collections:
  - name: community.docker
    version: "3.1.0"
    # Needs for rsync
  - name: ansible.posix
    version: "1.4.0"
  - name: kubernetes.core
    version: "2.3.2"
    # nomad_job
  - name: community.general
    version: "6.2.0"
